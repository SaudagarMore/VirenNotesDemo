Convert this function to an arrow function:
1)function add(a, b) {
  return a + b;
}
2)Write an arrow function isPositive that takes a number and returns true if positive, false otherwise.
4)Create an arrow function square that takes one parameter and returns its square.
5)Write an arrow function greet that takes a name and returns "Hello, name!".
6)Write an arrow function that takes no parameters and returns the string "Welcome".
7)Convert this to an arrow function:
function doubleArray(arr) {
  return arr.map(function(x) {
    return x * 2;
  });
}
8)Write an arrow function max that takes two numbers and returns the greater number.
9)Write an arrow function getFirstLetter that returns the first letter of a string passed to it.
10)Write an arrow function sumArray that takes an array of numbers and returns the sum using .reduce().

11)Create an empty array named colors and add "red" and "blue" to it using .push().
12)Given let nums = [10, 20, 30];, remove the last element using .pop() and store it in lastNum.
13)Given let fruits = ["apple", "banana"];, add "orange" to the beginning using .unshift().
14)Remove the first item from fruits using .shift() and save it in firstFruit.
15)Use .forEach() with an arrow function to log each element of the array nums.
16)Use .map() to create a new array that contains the squares of the numbers in nums.
17)Use .filter() to create a new array with only numbers greater than 15 from nums.
18)Use .find() to get the first number in nums that is greater than 15.
19)Check if nums includes the number 20 using .includes() and store the result in hasTwenty.
20)Use .slice() to create a new array with the first two elements of nums.